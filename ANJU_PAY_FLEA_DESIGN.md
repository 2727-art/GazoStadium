# AnjuPayフリマ 設計境界

## 目的

AnjuPayフリマは、画像を見せ合う場所ではなく、イラスト・実写・衣装コーデを言葉で紹介し、その文章と店主の人柄から「気になる人」を見つける一日棚です。最終目的は品物の取得ではなく、推しカードや本人が任意公開したXへ進み、プレイヤーが自分の意思で推したい相手を見つけることです。

購入で残るのはゲーム内の「出会いの記録」です。実物、画像データ、衣服、著作権、肖像権、利用権は移転しません。発送、DM、現金、電子マネー、外部通販を使った受け渡しは扱いません。

## 推し値市場との境界

推し値市場は、売り手がその場で時間と労力をかけて営業する参加型コンテンツです。AnjuPayフリマは非同期の発見導線であり、その代替にしません。

フリマから次の既存市場データを作成・更新・参照しません。

- `valueMarketStats`と市場ランキング
- 推し値市場の成約数、売上額、着手料、営業ターン
- 推し値商店、常連帳、肯定タグ、店主レポート
- 推し値証書、パトロン循環基金、推薦棚
- 市場実績、称号解除、推しカード成長

共用するのはFirebase Authentication、AnjuPayウォレットと増減台帳、App Check、公開済み推しカードの読取専用スナップショットだけです。

フリマ成立後に「直接ことばを聞きたいときは推し値市場へ」と案内できますが、常連帳への自動追加、優先マッチング、同じ店主との商談保証は行いません。

## 一日の流れ

1. 日本時間の日付をFunctionsが確定する。
2. 1プレイヤーにつき1日1件だけ出品できる。
3. 出品時に1 Payを支払う。取消、売却、非表示後も同日には再出品できず、出品料は返還しない。
4. 出品は次の日本時間0:00で公開終了する。正しさはクライアント時計やSchedulerへ依存せず、一覧と購入のたびにFunctionsが期限を判定する。
5. 入力中の文章は端末内の下書きとして残し、公開終了や通信失敗で消さない。
6. 連続出品報酬、欠席ペナルティ、秒刻みの終了煽りは設けない。

## 出品契約

- カテゴリは`イラスト`、`実写`、`衣装コーデ`の3つだけとする。
- タイトルは1行30文字以内、説明文は20〜240文字とする。
- 価格は10 Pay、25 Pay、50 Payの固定候補とする。
- タイトル、本文、店主名、公開推しカードの店主名・紹介文には、URL、メールアドレス、個人情報、外部連絡や実物取引の勧誘を書けない。Functionsが出品料を引く前に同じ基準で再検査する。
- 成人向け、未成年を性的に扱う内容、盗撮・性的ディープフェイクは本文へ掲載できない。武器などを題材にした創作説明は単語だけで拒否せず、違法・規制対象品の売買、発送、受け渡しを求める文脈を拒否する。
- 画像アップロード、画像枠、X本文や画像の埋め込み、oEmbed、OG取得、外部`fetch`は実装しない。
- Xは任意。`https://x.com/{handle}/status/{digits}`形式で、店主が自分の投稿として自己申告する個別ポストだけを専用欄へ設定できる。
- X URLはFunctionsがHTTPS、ホスト、パスを検証し、queryとfragmentを除いたcanonical URLだけを保存する。
- Xリンクを付けるには、同じhandleを店主の公開推しカードでも公開している必要がある。ただし、handle一致は運営による本人確認を意味しない。
- Xリンク公開時は、自分の投稿であること、必要な権利と写っている人の公開同意があること、成人向け・個人情報・実物または外部取引を含まないことへ同意する。
- フリマ画面内のXリンクは、任意ポストと共通推しカードのプロフィールを含め、外部移動前に「店主の自己申告であり、運営は本人確認も内容確認もしていない」ことを示して確認を取る。

X本文を取得しない設計のため、Functionsが確認できるのは「正規URLであること」と「公開推しカードのhandleと一致すること」までです。投稿IDが実際に店主の投稿か、リンク先の内容が安全か、後から削除・変更されたかは保証しません。自己申告、外部移動前の確認、購入後も使える固定理由の通報を組み合わせて扱います。

推し帳に保存するカードスナップショットからは`xHandle`を除外し、「追加した時点のカード」と表示します。店主がX公開を取り下げた後も、推し帳だけから古いXへ移動できる状態を作りません。

## 経済

- 出品料: 1 Pay
- 販売価格: 10 / 25 / 50 Pay
- 販売手数料: 価格の5%、端数切り上げ、最低1 Pay
- 買い手支払額: 表示価格
- 売り手受取額: 表示価格から販売手数料を引いた額
- 出品料と販売手数料は市場から回収し、新しいAnjuPayを発行しない。

出品と購入はFirestore transactionで、残高、台帳、出品状態、購入記録を一括確定します。価格、売り手、手数料、期限をクライアント入力から信用しません。自己購入、残高不足、売り手の残高上限、期限切れ、同時購入をサーバーで拒否します。

匿名認証とApp Checkだけでは、別UIDを使った実質的な自己売買を同一人物と判定できません。そのため価格を低い固定候補へ限定し、売上や成約数を公開指標や報酬へ使いません。

## Firestoreの正本
0:00付近では、各Firestore transactionの試行内でも現在時刻を取り直します。出品は要求開始時とtransaction試行時のJST日付が変わっていないこと、購入は試行時刻が`expiresAt`未満であることを再確認し、再試行中に日をまたいだ処理を確定しません。


- `anjuPayFleaListings/{listingId}`: UIDとJST日付から作る決定的ID。出品本文、期限、内部seller UID、状態を保存する。
- `anjuPayFleaSales/{listingId}`: 成約の一意な正本。再送と同時購入を判定する。
- `anjuPayFleaReceipts/{uid}/items/{listingId}`: 買い手と売り手それぞれの非譲渡の出会い記録。
- `anjuPayFleaFavorites/{uid}`: 非公開の件数メタデータ。transactionで100人上限を強制する。`sellers/{publicSellerId}`は本人だけの「推し帳」で、Xを含まない保存時点のカードを保持する。人数や登録者を公開しない。
- `anjuPayFleaReports/{reportId}`: 固定理由の通報。通報数は公開せず、通報だけで出品本体を自動非表示にしない。権利侵害、個人情報、成人向け、外部取引では、参考Xリンクだけを即時に安全確認中として隔離する。隔離前のcanonical X URLとhandleは、Rulesでクライアントから読めない運営確認用証跡として保持し、誤通報時に審査・復元できるようにする。

Firestore Rulesは上記をすべて直接読み書き不可とし、App Check強制の`anjuPayFleaAction`だけが、認証された本人へ必要な公開項目を返します。公開レスポンスへseller UID、buyer UID、reporter UIDを含めません。

当日の棚は`browseOrder`とdocument IDで安定順序を作り、上限付きのページとして返します。「続きを見る」は最後に受け取った公開listing IDをcursorにし、Functionsが同じJST日付の実在出品かを再検証します。応答件数の上限だけを理由に、出品料を支払った次ページ以降を見えなくしません。


## Callable操作

- `state`: 当日の棚、自分の出品、残高、推し帳、出会い記録、サーバー時刻と期限を返す。
- `browse_more`: 当日棚の次ページだけを返す。cursorは実在する同日出品へ限定し、日をまたいだ場合は最初からの更新を求める。
- `create_listing`: 決定的な当日枠を作り、出品料を一度だけ記帳する。
- `buy`: 買い手の支払、売り手の受取、販売手数料、売却状態、双方の記録を一つのtransactionで確定する。
- `cancel_listing`: 本人の公開だけを終了する。出品料は返還せず、当日枠を残す。
- `set_favorite`: 非公開の推し帳へ追加・解除する。
- `report`: `権利侵害`、`個人情報`、`成人向け`、`外部取引`、`その他`の固定理由を記録する。先頭4理由では、listing、成約正本、双方の出会い記録からXポストURLと店主カードのXアカウントを同じtransactionで除き、`xQuarantined: true`を返す。購入と同時実行されてもtransactionの再試行で、売却前・売却後のどちらの順序からもXなしの公開状態へ収束させる。

購入成功の再送は同じ結果を返し、残高と台帳を二重更新しません。別の買い手が同じ出品を同時購入した場合は、最初の1件だけが成立します。

## 画面原則

- トップでは推し値市場ボタンの隣へ並べず、「みんなの推しカード」の直後に独立パネルとして置く。
- 一覧には画像枠もXリンクも置かず、カテゴリ、タイトル、説明文の冒頭、店主名、価格を表示する。
- 詳細は説明文、任意のXポスト、店主の推しカード、購入判断の順にする。
- 購入確認で「届くもの: ゲーム内の出会いの記録」と「届かないもの: 実物・画像データ・衣服・権利」を同時に表示する。
- 成立後は店主の推しカードと任意のXを主役にする。
- 推し帳は非公開・最大100人で、登録人数、人気順、売上順、連続日数を表示しない。公開中の出品がない店主は「追加した時点のカード」と明示し、保存したXリンクは表示しない。


## 通報の運用

- 通報だけで出品本体を自動的に隠さない。通報数は出品者・通報者・他プレイヤーへ公開しない。
- 権利侵害、個人情報、成人向け、外部取引では、対象のXポストURLと店主カードのXアカウントだけをlisting、成約正本、買い手・売り手双方の出会い記録から除く。説明文、出品状態、購入結果、残高、ランキングは変更しない。
- 隔離前のX情報は公開文書や公開レスポンスへ戻さず、Admin専用の通報証跡だけに保持する。同じ通報の再送でも保存済み理由を基準に再検査し、旧データや競合で再混入したXを冪等に除く。
- 通報者の端末では、理由にかかわらず、読み込み済みの一覧全件（次ページ分を含む）、自分の出品、出会い記録、成立画面から対象のX情報を直ちに外す。
- 運営は`anjuPayFleaReports`の`listingId`から対象出品を照合し、権利侵害、個人情報、成人向け、外部取引を優先して確認する。
- 緊急性がある場合は、Admin権限の運用手順で対象`anjuPayFleaListings/{listingId}`を`hidden`へ変更する。クライアントへ管理用の非表示権限を渡さない。
- 誤通報や集団通報だけで残高・売上を巻き戻さない。返金や売上取消を導入する場合は、別の監査可能なtransactionと利用規約を先に設計する。
- X本文をゲーム内で取得しないため、リンク先確認は運営者が外部サイトへ移動する独立した審査行為として扱う。

## リリース前の確認

- Firestore Indexes、Rules、Functions、Hostingの順に配備し、IndexesがREADYになってからFunctionsを公開する。
- `anjuPayFleaAction`のApp Checkは初日から強制する。
- 2アカウントで出品料、購入、売り手入金、台帳、失効、取消、同時購入を確認する。
- フリマ操作の前後で`valueMarketStats`、実績、推し値商店、証書、推しカード成長が変化しないことを確認する。
- Xリンクをゲーム内で取得・展開・埋め込みしていないことを確認する。
- 390pxとデスクトップ幅で、本文、確認、外部リンク、推しカード、戻る操作を確認する。
