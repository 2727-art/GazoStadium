# AIと対戦しよう 文字コラトレーニング

Status: implementation contract
Scope: solo training, 5〜10 image roster and AI DRAW, beat characters, support-script marketplace, sales ranking, permanent cosmetic customization
Out of scope: P2P, camera/motion judging, medical or fitness claims

## 1. Product promise

「AIと対戦しよう 文字コラトレーニング」は、対戦相手を待たずに、好きな画像を5〜10枚登録し、その中から5枚をDRAWして短時間の運動を始められる完全ソロモードである。

- 画像はAI対戦相手の「顔」
- ママ／いもうと／お姉ちゃんはAIの「性格とテンポ判断」
- 応援台本はAIが表示する「言葉」
- BPMはAIが仕掛ける「運動テンポ」
- ビートキャラクターはAIの「音の人物像」
- プレイヤーの実際の動作・回数・フォームは判定しない

既存のP2Pコンテンツ「鍛え合い60」とは、画面、状態、Firebaseデータ、決済、記録を分離する。既存モードの5画像処理・0または40〜160 BPM・Wake Lock・音声再開の知見は再利用するが、P2Pルームや対戦結果へ接続しない。

## 2. MVPのプレイループ

1. 5〜10枚の画像を端末内で登録し、各画像へ `0` または `40〜160 BPM` を設定する。
2. 運動、1ラウンドの長さ、AI性格、ビートキャラクター、端末内メトロノーム音量を選ぶ。
3. 購入済みの窓と応援メッセージ装飾を選ぶ。未購入品はこの画面で試着できる。
4. 無料の標準応援、または市場の有料応援台本を選ぶ。
5. 6〜10枚の場合は画像内容を解析せず、重複なしで5枚をDRAWする。順番とBPMを確認し、必要なら決済前に引き直す。
6. 3秒カウントダウン後、5ラウンドを進める。
7. 各ラウンド終了時に反応を選び、次のBPMを確認して、自分で次へ進む。
8. 5ラウンド完走後、実測できる到達ラウンド数・運動時間・使用台本・今回の5枚を表示する。

MVPの運動候補:

- その場足踏み: 1拍ごとに片足を交互に動かす目安
- スクワット: 2拍で1回の目安
- 壁プッシュ: 2拍で1回の目安
- 好きな運動: BPMはテンポの目安だけにする

ラウンド時間は15秒、20秒、30秒、45秒、60秒から選択でき、初期値は20秒、1ラウンドの上限は60秒とする。5ラウンドすべてを60秒にした場合の最大運動時間は5分である。反応選択中、次ラウンド確認中、一時停止中、開始・再開カウント中は運動時間へ含めず、終了操作までに実際に進んだ現在ラウンドの端数秒は結果へ含める。

## 3. 画像とプライバシー

- 画像は既存の端末内画像処理で縮小・WebP化する。
- 画像はFirebase、Functions、応援台本市場へ送らない。
- 選択画像を保存しないのが初期設定。
- 「この端末にロスターを保存」を明示的に選んだ場合だけ5〜10枚をIndexedDBへ保存する。
- 旧record schema 1の5枚は読み取り互換とし、次に保存する時だけschema 2へ書き戻す。DB構造は変えないためIndexedDBのdatabase versionは1を維持する。
- ローカル保存は同じブラウザから削除できる。
- 削除に失敗した場合は失敗を表示し、保存同意をOFFへ見せかけずONのままにする。
- 応援台本の作者・購入者・ランキング参加者へ画像は一切渡らない。

## 4. AI性格とBPM

3つは難易度名ではなく、テンポ調整の性格である。

### 4.1 共通ルール

- 5枚登録時は選択順のまま1〜5ラウンドへ割り当てる。
- 6〜10枚登録時は一様ランダム・重複なしで5枚と順番をDRAWし、開始前に画像、順番、基準BPMを本人へ表示する。
- DRAWは画像の内容・ファイル名を解析しない。画像、画像ID、BPM、乱数値、DRAW結果をFirebase、Functions、作者、ランキングへ送らない。
- 引き直しでは有料台本を消費しない。DRAW確定後に、既存の残高・価格・支払後残高・任意性を確認してから利用を開始する。
- 開始済み有料利用のDRAW結果は同じ利用を復旧できるよう`localStorage`へ固定し、再読込で再抽選しない。画像本体は明示同意時だけIndexedDBへ置く。
- 同じ有料`useId`の保存済みロスター枚数、DRAW、AI性格、BPM、計画、終了状態を検証できない場合は、新しいDRAWや計画へ置き換えず復旧を停止する。追加請求は行わない。
- 復旧を停止した有料利用は、本人が使い切り・返金なしを明示確認した時だけ`exited`として自主終了できる。終了記録に失敗した場合は同じ`useId`の再送だけを許可し、別の有料利用や追加請求へ進めない。
- 起動時はサーバーの開始済み利用確認と端末ロスター復元が収束するまで開始操作を無効にする。市場がオフラインと確定した場合だけ、開始済み有料利用を取り込まず無料トレーニングを許可する。
- 10枚の第1セッションを完走した場合だけ、未登場の補集合5枚による第2セッションを一度提示する。第2セッション完走後は同じ一巡の「残り5枚」を再表示しない。
- 残り5枚も新しい5ラウンドセッションであり、新しい実績actionIdを使う。有料台本は自動課金せず、価格確認と新しいuseIdが必要。
- 安全停止時は同じ5枚・残り5枚の再戦操作を表示しない。
- 画像の基準BPMは `0` または `40〜160` の整数。
- `0 BPM` は `FREE RHYTHM`。どの性格でも自動的に有拍へ変更しない。
- BPM変更はラウンド中に行わず、ラウンド境界でだけ確定する。
- 確定済みの次BPMはセッションへ保存し、再読込で抽選・補正をやり直さない。
- 台本へ渡す「上がった／下がった／同じ」は、基準値や補正値ではなく、実際に表示・再生するBPMの比較結果から求める。

### 4.2 ママモード

「きつい（まだ続けられる）」を選ぶと、次の有拍ラウンドは現在の実効BPMより10遅い値を目標にする。

```text
next = min(nextBaseBpm, currentEffectiveBpm - 10)
next = clamp(next, 40, 160)
```

- 現在がFREE RHYTHM、または次画像がFREE RHYTHMなら、無理に比較・変換しない。
- 下限40でこれ以上下げられない場合は40のままにし、「同じ／下限」用の台詞を選ぶ。
- 「余裕」「ちょうどいい」では、次画像の基準BPMを使う。

### 4.3 いもうとモード

開始時に第2〜5ラウンドの補正を `-20 / -10 / 0 / +10 / +20` から抽選する。

- 同じ補正を連続させない。
- 抽選結果はセッション作成時に固定する。
- 実効BPMは次画像の基準BPMへ補正を加え、40〜160へ収める。
- 反応回答では補正を変更しない。
- 基準値0は補正せずFREE RHYTHMのまま。

### 4.4 お姉ちゃんモード

「きつい（まだ続けられる）」を選ぶと、次の有拍ラウンドは現在の実効BPMより10速い値を目標にする。

```text
next = max(nextBaseBpm, currentEffectiveBpm + 10)
next = clamp(next, 40, 160)
```

- 上限160でこれ以上上げられない場合は160のままにし、「同じ／上限」用の台詞を選ぶ。
- 「余裕」「ちょうどいい」では、次画像の基準BPMを使う。
- 選択画面と反応画面の両方に「きついと答えると次は速くなります」と明記する。

### 4.5 ラウンド境界

```text
運動終了
→ メトロノーム停止
→ 反応選択
→ 次画像と実効BPMを確定・予告
→ プレイヤーが「次へ」を押す
→ 3秒カウント
→ 次ラウンド
```

反応は次の3つ:

- 余裕
- ちょうどいい
- きつい（まだ続けられる）

第1〜4ラウンドの反応だけが次へ影響し、第5ラウンドは記録のみ。回答にはラウンド番号を付け、二重適用しない。

### 4.6 ビートキャラクター

次の5種を無料の固定カタログとして提供する。

| ID | 表示名 | 音の印象 |
| --- | --- | --- |
| `heavy_pulse` | 重厚パルス | 低く太く、落ち着いた印象 |
| `soft_bell` | まろやかベル | 丸く柔らかい印象 |
| `clear_tap` | クリアタップ | 現行440Hz短音と互換の標準 |
| `glass_spark` | グラススパーク | 高く軽快な印象 |
| `machine_ai` | 無機質AI | 電子的で無機質な印象 |

- 年齢・性別を断定する商品名にせず、音の印象として表現する。
- 任意の音声ファイル、URL、周波数、音量を利用者や台本作者から受け取らず、運営固定のWeb Audio合成レシピだけを使う。
- 選択は端末設定へ保存し、運動開始時にセッションへ固定する。途中変更は次のセッションから反映する。
- 試聴は本人が「音を試す」を押した時だけ96 BPMで短く再生し、自動再生しない。
- 文字コラ専用の初期音量は36%。準備画面の0〜100%スライダーで調整し、値はこの端末だけへ保存する。変更操作だけでは再生を始めない。
- ヘッダーのSE設定とは独立させ、文字コラの試聴とトレーニング中のメトロノームだけへ反映する。他モードの音量初期値は変更しない。
- ラウンド開始時は同じBPMでも3秒後の新しい`effectiveAt`へ拍を張り直す。カウント中は無音、境界の最初の拍から選択音色とする。
- `0 BPM`、ミュート、一時停止、画面非表示、終了では無音を維持する。音声失敗時も運動は進行できる。
- ビートキャラクターはBPM、AI性格、台本、運動時間、実績、ランキング、AnjuPayへ影響しない。

## 5. 停止・一時停止・再開

「きつい」と安全停止を同じ操作にしない。

- 常設: `一時停止`
- 常設・強調: `無理・痛い・めまい／即停止`
- 即停止は1操作で音、時計、アニメーション、Wake Lockを止める。
- 即停止に敗北、減点、AnjuPayペナルティを付けない。
- 痛み、めまい、息苦しさ、体調不良がある場合は中止するようシステム文言で案内する。
- AI台本は安全停止UIやシステム安全文言を上書きできない。
- タブが非表示になったら自動一時停止する。
- 復帰・再読込後に自動再生しない。「3秒後に再開」を本人が押した時だけ再開する。

## 6. 応援台本

### 6.1 商品単位

- 1商品は1つのAI性格専用。
- 有料利用の開始後は、その商品に固定されたAI性格を変更できない。
- 商品は題名、紹介、性格、価格、台詞セット、改訂番号を持つ。
- 作者が変更できるのは文字だけ。BPM、運動、時間、停止ボタン、安全案内は変更できない。
- 購入時は台本の改訂番号と全文スナップショットを利用記録へ固定する。
- 作者が後で編集・非公開化されても、開始済みの利用内容は変わらない。

### 6.2 必須スロット

各スロットへ2〜4行、合計28〜56行を入力する。1行は4〜42文字。

| ID | 使用場面 |
| --- | --- |
| `start` | セッション開始・ラウンド開始 |
| `active_free` | 通常区間、0 BPM |
| `active_low` | 通常区間、40〜79 BPM |
| `active_mid` | 通常区間、80〜119 BPM |
| `active_high` | 通常区間、120〜160 BPM |
| `final_free` | ラウンド終盤、0 BPM |
| `final_low` | ラウンド終盤、40〜79 BPM |
| `final_mid` | ラウンド終盤、80〜119 BPM |
| `final_high` | ラウンド終盤、120〜160 BPM |
| `tempo_down` | 次ラウンドが実際に遅くなる |
| `tempo_same` | 同じ、FREE継続、上下限で変化できない |
| `tempo_up` | 次ラウンドが実際に速くなる |
| `rest` | 反応・呼吸・次ラウンド確認 |
| `clear` | 5ラウンド完走 |

使用可能なプレースホルダー:

- `{bpm}`
- `{round}`
- `{remaining}`

HTML、URL、未知のプレースホルダーは許可しない。表示時は必ずテキストとして扱う。

### 6.3 選択順序

1. 現在ラウンドの実効BPMを確定する。
2. `FREE / LOW / MID / HIGH` の実テンポ帯を求める。
3. 残り時間から `ACTIVE / FINAL` を求める。
4. 直前ラウンドとの実効BPM比較から `DOWN / SAME / UP` を求める。
5. 対応スロットから、直前と同じ行を避けて選ぶ。
6. ラウンド間の本人操作待ちには `rest`、5ラウンド完走結果には `clear` を必ず表示する。

低BPMの最終区間は `final_low` を使うため、「最後の追い込み」のような高速向け文言が誤って表示されない。

視覚メッセージはBPMへ寄せて約2.4〜5.2秒ごとに更新する。スクリーンリーダーの読み上げは別の領域で頻度を落とし、ラウンド開始、テンポ変化、残り5秒、完走、安全メッセージだけを通知する。

### 6.4 UGC安全基準

公開不可:

- 痛み・めまい・息苦しさを無視させる
- 水分、休憩、中止、一時停止を妨げる
- 危険な回数・速度・姿勢を強要する
- 人格否定、差別、脅迫、性的表現、特に家族役の性的表現
- 個人情報、連絡先、URL、外部取引・外部決済への誘導
- HTML、スクリプト、制御文字

公開前と利用確認前に全文プレビューと全テンポ帯シミュレーターを表示する。支払い対象は台詞の秘密情報や所有権ではなく、BPMへ同期した5ラウンド自動演出の1回利用である。自動検査は補助であり、通報・運営非表示の経路も用意する。

## 7. 買い切り演出カスタマイズ

応援台本の1回利用とは別に、トレーニング画面をその日の気分へ合わせる永久利用の演出セットをAnjuShopで販売する。演出セットを使わなくても、無料の標準表示ですべての運動機能を利用できる。

### 7.1 商品と解放単位

| 商品ID | 表示名 | 価格 | 解放内容 |
| --- | --- | ---: | --- |
| `ai_training_style_soft_glow` | ソフトグロウ | 300 Pay | ソフトグロウの窓1種＋応援メッセージ装飾1種 |
| `ai_training_style_neon_beat` | ネオンビート | 500 Pay | ネオンビートの窓1種＋応援メッセージ装飾1種 |
| `ai_training_style_crimson_azure` | 韓堕ちダークネオン | 800 Pay | 黒・濃紺＋赤青ネオン＋太極リアクター風ラインの窓1種＋黒ガラスの応援メッセージ装飾1種 |
| `ai_training_style_stardust_stage` | スターダストステージ | 1,000 Pay | スターダストステージの窓1種＋応援メッセージ装飾1種 |

- 1回のショップ購入で、そのセットの窓と応援メッセージ装飾を永久に解放する。消費、使用回数、期限、維持費は設けない。
- 窓と応援メッセージ装飾は別スロットとして扱う。購入済みのセット同士なら、たとえばソフトグロウの窓とネオンビートのメッセージを組み合わせられる。
- 4セットをすべて購入した場合、無料の標準を含む窓5種×メッセージ5種の最大25通りを選べる。
- 無料の標準窓と標準メッセージ装飾は常に選べる。
- 応援台本は従来どおり5ラウンド1回の使い切り商品である。演出セットの購入に台本の利用権を含めず、台本利用でも演出セットを消費しない。
- 演出は画面の見た目だけを変える。BPM、音、ラウンド時間、AI性格の判断、応援台詞の選択、結果、戦績、ランキング、AnjuPay報酬へ影響しない。
- 一時停止、即停止、体調注意、残り時間、フォーカス表示など、操作と安全に必要なUIの意味・優先度・可読性をテーマで変更しない。

### 7.2 試着・装着・保存

- 未購入の窓とメッセージ装飾も準備画面の見本へ個別に試着できる。試着はメモリ上だけで、購入状態や保存済み装着を変更しない。
- 未購入品を含む組み合わせは保存できず、そのまま運動セッションへ持ち込まない。「購入して解放」または「試着を取り消す」を明示する。
- 購入済み品だけの組み合わせは、本人操作で装着保存できる。クライアント表示だけで購入済みとは判断しない。
- 装着保存は専用Callable `save_cosmetics` が2つの商品IDを固定カタログで検証し、購入済み商品をサーバー側で照合してから `aiTextTrainingPreferences/{uid}` へ記録する。
- 非公開設定は本人用の`state`応答にだけ含め、Firestoreクライアントからの直接read/writeを許可しない。公開ランキング、台本作者、他プレイヤーへ装着内容を渡さない。
- 保存済み商品が将来無効化された場合や不明なIDを受け取った場合は、そのスロットだけ無料の標準表示へ戻す。
- 運動開始時に有効な装着をローカルセッションへ固定し、再読込後も同じ見た目で再開する。別タブや設定変更で進行中セッションの見た目を途中変更しない。

## 8. 使い切り市場

### 8.1 価格と手数料

MVPの固定価格:

- 5 Pay
- 10 Pay
- 25 Pay

経済吸収:

- 新規公開・改訂公開: 1 Pay
- 利用成立手数料: 20%、最低1 Pay
- 残りを作者へ付与

無料の標準応援は常に選べる。有料台本は運動機能やデイリー完走を解除する必須課金ではない。

### 8.2 一回利用の定義

- 1回利用 = 5ラウンドの1セッション。
- 1メッセージ、1ラウンドごとには課金しない。
- 5枚のDRAWと順番・BPM確認が終わった後、「N Payでこの応援を使って開始」で決済する。
- 決済と利用開始記録を同じFirestoreトランザクションで行う。
- `actionId` はクライアントが生成し、再送しても同じ利用を返して二重請求しない。
- 同じ購入者が同時に持てる有料の開始済み・未終了利用は1件までとし、買い溜めを作らない。
- 開始後の自主終了・即停止は利用済みとなるが、追加請求やペナルティはない。
- 通信切断、再読込、ブラウザクラッシュでは同じ `useId` を再開し、再課金しない。
- 終了通知がFunctionsで確定するまでは同じ利用を再プレイできず、再送だけを許可する。終端状態の `useId` は再開できない。
- Functionsが決済を確定できなければ運動を開始せず、請求もしない。
- 決済成功後に端末画像を失った場合、開始時と同じロスター枚数と順番で選び直し、保存済みDRAWを再抽選せず同じ利用を再開できる。

### 8.3 残高上限

- 購入前に購入者残高、価格、購入後残高、使い切り条件を確認画面へ表示する。
- 決済トランザクションは確認画面の購入者残高も照合し、別タブ等で変化した場合は課金せず再確認へ戻す。
- 公開・改訂の1 Payも同様に、公開確認画面の残高をトランザクションで照合する。
- 作者の受取残高が上限に達する場合は利用開始前に停止し、購入者から引かない。
- 自分の台本は無料試遊できるが、売上・利用数・ランキングへ加算しない。

## 9. 売上ランキング

ランキングの役割は「人気と継続的な応援の可視化」であり、Pay報酬競争にしない。

- 初期表示: 月間
- 切替: 累計
- 主順位: ランキング対象売上
- 補助表示: 実売上、利用回数、ユニーク購入者数
- 同一購入者→同一作者はJST同日1回だけランキングへ加算
- 実際の決済・作者受取・市場手数料は毎回そのまま処理する
- 自己利用はランキング対象外
- 順位によるPay配布や購入者へのPay還元は行わない
- 月間終了時の上位は名誉表示として履歴へ残せる

「ランキング対象外でも決済と作者受取は通常どおり」と画面へ明記する。

## 10. Xプロフィール

- 応援台本ランキング専用の公開設定を持つ。他市場の公開設定を自動継承しない。
- 入力はXユーザー名だけ。形式は `[A-Za-z0-9_]{1,15}`。
- URLはクライアントで `https://x.com/{handle}` として組み立てる。
- ランキング行だけに任意表示する。
- `target="_blank"`, `rel="noopener noreferrer nofollow ugc"`, `referrerpolicy="no-referrer"` を付ける。
- 外部遷移前に「作者の自己申告・本人未確認・リンク先未確認」を確認する。
- X API、埋め込み、プロフィール画像取得、フォロワー数取得を行わない。
- 公開OFFまたはユーザー名削除後は、過去ランキングの順位を残したままXリンクだけを即時非表示にする。
- 通報で安全上の問題がある場合は、台本と公開Xを同時に隔離できる。
- 通報は `presetId + revision + reporter` 単位とし、閲覧した改訂番号をFunctionsで照合して、その全文スナップショットを保存する。送信前に改訂された場合は受理せず全文確認へ戻す。改訂後は同じ報告者も再通報できる。
- `危険な運動指示 / 性的表現 / 個人情報` は、通報対象の改訂版を実際に利用した異なる3人がその改訂を通報した時点で台本を非公開にし、作者のランキングXリンクもOFFにする。別の改訂だけを利用した人、未購入者、その他理由の通報も証跡として保存するが、自動隔離数には加えない。
- 隔離後は同一台詞のまま改訂番号だけを進めて再公開できない。作者が応援台詞を見直して新しい改訂として再公開するまでXリンクも再公開できず、改訂後も作者の明示的な再同意を必要とする。

## 11. Firebaseデータ境界

すべてFunctions callable経由。Firestoreクライアント直接書き込みは禁止する。

```text
aiTextTrainingPresets/{presetId}
aiTextTrainingPresets/{presetId}/revisions/{revisionId}
aiTextTrainingSellerProfiles/{uid}
aiTextTrainingUses/{useId}
aiTextTrainingActiveUses/{uid}
aiTextTrainingAchievementSessions/{sessionId}
aiTextTrainingActiveAchievementSessions/{uid}
aiTextTrainingPlayerStats/{uid}
aiTextTrainingPublishActions/{actionId}
aiTextTrainingSellerStats/{uid}
aiTextTrainingMonthlyPeriods/{YYYY-MM}/entries/{uid}
aiTextTrainingRankingPairs/{YYYY-MM-DD_pairHash}
aiTextTrainingSellerBuyerPairs/{pairHash}
aiTextTrainingMonthlyBuyerPairs/{monthPairHash}
aiTextTrainingPresetRevisionBuyers/{revisionBuyerHash}
aiTextTrainingReports/{reportId}
aiTextTrainingPreferences/{uid}
```

主なCallableアクション:

- `state`
- `browse`
- `publish`
- `unpublish`
- `start_paid_use`
- `resume_use`
- `finish_use`
- `begin_achievement_session`
- `finish_achievement_session`
- `save_profile`
- `save_cosmetics`
- `rankings`
- `report`

公開レスポンスへUIDや装着設定を含めない。作者は決定的な公開IDで表し、サーバー内部のUIDと分離する。`aiTextTrainingPreferences`は本人用Callableだけが読み書きし、装着可能商品はサーバー権威の商品カタログと購入済み記録から判定する。

### 11.1 実績コレクション

文字コラトレーニングと応援台本販売は、競技実績・鍛え合い60実績・VALUE MARKET実績から分離した2カテゴリで表示する。AnjuPay報酬、RATE、順位、BPM、運動強度は解除条件にしない。

- `文字コラトレーニング`
  - 5ラウンド完走回数: `1 / 5 / 20 / 50 / 100 / 300`
  - 5ラウンドを完走した異なるJST日数: `3 / 7 / 14 / 30 / 60 / 100`
- `応援台本の販売`
  - ランキング対象利用回数: `1 / 3 / 10 / 30 / 100 / 300`（同じ利用者からはJSTの1日1回）
  - 異なる有料利用者数: `3 / 10 / 30 / 100`（生涯のユニーク利用者数）

トレーニング実績は、最初のラウンドの3秒カウントダウンを始める時にだけ`begin_achievement_session { actionId, modeId, roundSeconds }`を送る。準備画面と`aiTextTrainingPreview`によるローカル画面プレビューでは開始しない。サーバーが返した`session.id`を通常のセッション復元情報にも保存し、有料応援の再読込復元後も同じ実績セッションを使う。

開始はUIDごとにサーバー権威の30秒間隔で制限し、同じ`actionId`の冪等再送だけは制限中も受け付ける。異なる開始で置き換えた旧セッションは完走扱いにせず、実績セッション文書には開始24時間後のFirestore Timestampを`deleteAt`として保存し、TTLポリシーで自動削除する。

結果では有料台本の`finish_use`とは独立して、`finish_achievement_session { sessionId, outcome, completedRounds, activeSeconds }`を送る。`completed / safety_stopped / exited`をすべて終端として記録するが、実績進捗へ加えるのは、開始時のラウンド秒数と整合する5ラウンド完走だけとする。安全停止・途中終了は既存進捗や解除済み実績を減らさない。カメラ、モーションセンサー、自己申告の回数・フォームは判定根拠にしない。

通信失敗は運動、一時停止、安全停止、有料利用の終了処理を妨げない。未送信記録は`ownerState / ownerUid / actionId / modeId / roundSeconds / sessionId / outcome / completedRounds / activeSeconds`だけを端末の`localStorage`再送キューへ最大12件保存し、画像、画像参照、5枚のBPM、台本ID、台詞本文、演出設定を含めない。Firebase UIDが未確定なら`ownerState: pending`として保存し、最初に確定したUIDへ一度だけ束縛する。束縛済みレコードを別UIDへ付け替えず、現在の認証UIDと一致する記録だけを送る。別UIDの記録と送信に失敗した記録はキューへ残しつつ、現在UIDに属する後続記録の再送を妨げない。結果画面の手動再送と、次回認証接続時の自動再送を用意する。解除応答の`newlyUnlocked`は共通の`hariai-achievements-unlocked`通知へ渡し、表示後のACKはベストエフォートとする。

導入前の無料トレーニングは、サーバーで開始・完走時間を検証できる履歴がないため遡及加算しない。台本販売側は既存のサーバー正本`aiTextTrainingSellerStats`にあるランキング対象利用回数と異なる購入者数を実績コレクション取得時に再評価し、導入前から成立済みの販売を失わせない。

## 12. クライアント状態

```text
setup
├─ cosmetic-preview
├─ support-browse
├─ support-detail
├─ author-editor
├─ author-preview
└─ rankings

setup → countdown → playing → reaction → next-preview
              ↑          │            │
              └─ resume ─┘            └→ countdown

playing → paused → countdown → playing
playing/paused → safety-stopped → result
reaction after round 5 → result
```

保存するセッション状態:

- schema version
- local session ID / paid use ID
- selected AI mode, exercise, round length
- purchased cosmetic selection fixed at session start
- image references or re-selection requirement
- five base BPM values
- effective BPM values already resolved
- fixed いもうと offsets
- current round, remaining milliseconds, phase
- round-numbered reactions
- immutable script snapshot
- paid/free marker and completion state
- achievement action ID / server session ID / begin-requested marker
- roster count / fixed DRAW indices / first-or-second-leg marker
- fixed beat character ID

未購入品の試着状態は保存するセッション状態へ含めない。

## 13. Accessibility and device behavior

- 主要操作は最低44px、停止操作は54px以上。
- キーボード操作と明確なfocus表示。
- 色だけでBPM帯・状態を伝えない。
- `prefers-reduced-motion` ではクロスフェード、脈動、カットイン移動を止める。
- 音は開始タップで初期化し、拒否時は無音でも進行できる。
- 音色試聴も本人操作だけで開始し、音色名を含む個別の試聴ラベルを付ける。
- メトロノーム音量はラベル、現在値、0〜100%のキーボード操作可能なスライダーを備える。
- メトロノームON/OFFを選べる。
- Wake Lockは運動中かつ表示中だけ取得する。
- 画面非表示、一時停止、結果、終了で音とWake Lockを解放する。
- 画像代替テキストは「ラウンドNの選択画像」とし、作者や台本へ画像内容を推測させない。
- 演出テーマは`prefers-reduced-motion`と強制カラーモードへ追従し、装飾を無効化しても操作名、状態、応援本文を読める。
- 演出テーマの疑似要素は入力を受け取らず、安全UI、残り時間、フォーカスリングを覆わない。

## 14. 完了条件

- 既存P2Pモードを開始・終了でき、状態が混ざらない。
- 5〜10画像を登録し、重複なしの5枚DRAW、10枚の補集合DRAW、全BPM境界 `0 / 40 / 160` を扱える。
- 旧5枚ロスターを読み取れ、次回保存で10枚対応schemaへ遅延移行できる。
- DRAW前の引き直しで課金せず、開始済み有料利用の復旧では5枚・順番・BPMが変わらない。
- 10枚の残り5枚は完走後に一度だけ提示し、安全停止では再戦を促さない。
- 残り5枚の有料再戦は残高・価格・支払後残高・任意性を再表示し、新しい利用としてのみ開始する。
- 5つの固定ビートキャラクターを明示試聴でき、未知IDはクリアタップへ戻る。
- 文字コラのメトロノームは初期36%で、0〜100%の端末内設定を再読込後も復元し、他モードへ波及しない。
- 同BPMの次ラウンドでも3秒カウント中は無音で、境界拍から選択音色が鳴る。
- 15 / 20 / 30 / 45 / 60秒を選択でき、1ラウンドが60秒を超えず、5ラウンド60秒設定の運動時間が最大5分になる。
- 一時停止、カウント、反応待ちを運動時間へ加算せず、途中終了時は現在ラウンドの実運動時間だけを結果へ加算する。
- 実績記録を最初のカウントダウンで一度だけ開始し、完走・安全停止・途中終了を有料利用終了と独立して冪等に確定できる。
- 5ラウンド完走だけが文字コラ実績へ加算され、安全停止・途中終了で既存進捗が減らない。
- 実績通信失敗が運動を止めず、画像・BPM・台詞を含まない端末内キューから結果画面または次回接続時に再送できる。
- 旧無料プレイを遡及せず、既存の台本販売統計は販売実績へ反映する。
- 3性格の実効BPMが仕様どおりで、再読込しても変わらない。
- お姉ちゃんの注意と常設即停止がある。
- 低BPM終盤へ高速追い込み台詞が混ざらない。
- UGCがHTML/BPM/安全UIを操作できない。
- 4つの買い切り演出セットがカタログ価格どおり販売され、各セットの窓とメッセージ装飾を別々に組み合わせられる。
- 未購入演出は試着できても保存・セッション使用できず、購入済み演出だけをCallableの所有権検証後に非公開設定へ保存する。
- 演出変更がBPM、時間、AI判断、結果、安全UIへ影響せず、不明・利用不可な商品IDは標準表示へ安全に戻る。
- 使い切り決済が同じactionIdで二重請求されない。
- 終了待ち・終端済みの同じ有料利用を追加決済なしで再プレイできない。
- 決済確認時の残高が取引時にも一致し、変化時は再確認になる。
- 公開確認時の残高も取引時に一致し、変化時は再確認になる。
- `rest` と `clear` を含む販売台本の全必須枠が実プレイで使われる。
- 改訂別の利用証跡・通報証跡と、その改訂を利用した3人による高危険度通報時の台本・X隔離が機能する。
- 自己利用、同日同一ペア、通常利用のランキング集計が分離される。
- Xは個別同意、形式検証、外部確認、公開解除を満たす。
- Firestore Rules、Indexes、App Check配線、純粋ロジック、Functions取引、フロント配線のテストが通る。
