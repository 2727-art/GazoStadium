# 鍛え合い60

## 目的

画像の魅力を採点する既存モードとは別に、好きな画像を「にんじん」にして、知らない相手と60秒ずつ運動を続ける1対1のロールプレイモードを提供する。

- モードID: `training`
- バリアント: `kitaeai_60`
- プロトコル: `1`
- 競技RATE、公開ランキング、王座、期間戦績報酬の対象外
- カメラ、マイク、動作センサーは使わず、完遂は本人の自己申告

## 1セットの流れ

1. 両者が表示名、好きな画像1枚、運動強度、配慮してほしい条件を準備する。
2. 先攻の指示役が、運動名、完了条件、短い掛け声、任意のBPMを送る。
3. 実行役が開始すると、相手の画像上に60秒のプログレスバーを表示する。
4. 実行役は「コンプリート」または「ギブアップ」を選ぶ。期限到達はタイムアウト負けとなる。
5. コンプリートした実行役が次の指示役へ交代する。
6. 2ターンごとに双方が継続を選んだ時だけ次セットへ進む。どちらかが終了を選ぶか、最大6ターンに達すると相互完遂で終了する。

ギブアップまたはタイムアウトした実行役が負け。双方が最後まで区切りよく終えた場合は勝敗なしの相互完遂とする。

## 指示データ

- 運動名: 40文字以内
- 完了条件: 60文字以内
- 掛け声: 120文字以内
- BPM: 未指定または40–160
- 1回あたりの拍数: 1、2、4
- 最大3往復（6ターン）

このモードは、互いに現実的な指示を出して役になりきる信頼ベースの遊びとして設計する。
痛み、めまい、体調の変化を感じた場合は勝敗より中止を優先し、ギブアップはその一戦の負けとしてだけ扱う。

## 同期とプライバシー

- 公開待機列へ置くのは匿名UID、端末セッションID、表示名、運動強度、プロトコル、時刻、待機状態だけとし、配慮条件は置かない。
- 全体マッチングlockはルームと招待を安全に作る短時間だけ保持し、招待作成後すぐ解放する。1セッションが受け取れる招待は同時に1件までとし、招待中のプレイヤーは別ルームのホストになれないため、4人以上なら独立した2組目を続けて成立できる。
- 配慮条件は招待を受けた本人が、参加者2人だけ読めるルームへ承諾と同時に書き込む。
- マッチング、配慮条件、指示、時刻、完遂・ギブアップ、継続投票だけをRealtime Databaseへ保存する。
- 画像は1280px以内のWebPへ端末内変換し、WebRTC DataChannelで相手へ直接送る。Firebaseへ保存しない。
- 両者の画像交換が45秒以内に完了しなければ、指示ターンを始めずNO CONTESTとして終了する。
- 1ルーム1接続とし、シグナルは宛先本人だけが読める領域へ分離する。
- メトロノームは自由卓の共通コントローラを再利用し、双方の拍を同期する。タイマーの正本はRealtime Databaseのサーバー時刻とする。

## サーバー確定

Cloud Functionsの`trainingAction`がルーム参加者、役割交代、各ターンの開始・終了時刻、継続投票を検証し、結果と完遂セット数を再導出する。確定結果はユーザー専用の`trainingProfiles`へ保存し、同じルームの二重計上は`trainingClaims`で防ぐ。

表示する継続記録は、参加回数、完遂セット数、完遂日数、現在の連続日数、最長連続日数。1セット以上の完遂は専用デイリーミッションの進捗にのみ加算する。

## 一時データの保持と清掃

`cleanupTrainingRooms`を5分ごとに実行し、`createdAt`カーソルで1回最大100ルームずつ巡回する。自然に決着したルームは、先に両参加者のプロフィール・claim・デイリーをサーバー確定し、失敗時は削除せず次回へ残す。

- `serverFinalized`済みルームは、結果画面の再読込確認に備えて確定時刻から24時間保持してから削除する
- `destroyed`または`expired`のルームは、指示、配慮条件、signalsを長く残さないため、サーバーtombstone時刻から1時間で削除する
- プレゼンスのない`forming`が6時間、`active`が24時間を超えた場合は、まずtombstone化して一致するactive・invite・match lockだけを解放し、1時間後に削除する
- 待機列は`sessionId`で別タブ・別端末を区別し、`lastSeen`から10分を超えた項目を同じ定期処理で1回最大100件削除する

active、invite、待機列の解放は現在のroom／session一致をトランザクションで再確認する。古い接続の切断処理が、新しい端末の予約や招待を消してはならない。

## 終了したふたりチャレンジとの境界

旧`team`系の待機列、招待、ルーム、チャット、採点領域は新規書き込みを禁止する。過去の戦績、実績、購入済み称号、デイリー受取記録は削除せず「終了モードの記録」として保持する。
