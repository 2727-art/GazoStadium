# 鍛え合い60

## 目的

画像の魅力を採点する既存モードとは別に、自分も相手も3セットずつ鍛え、休憩中は相手を応援する1対1のロールプレイモードを提供する。画像は勝敗の採点対象ではなく、今日の運動を続けるための「にんじん」として使う。

- モードID: `training`
- バリアント: `kitaeai_60_v2`
- プロトコル: `2`
- 1人3セット、2人で3往復・6ターン固定
- 競技RATE、公開ランキング、王座、期間戦績報酬、共通実績の対象外
- カメラ、マイク、動作センサーは使わず、完遂は本人の自己申告

## にんじんの秘密選択

両者の画像交換後、各プレイヤーは自分が実行役になる3セットで見る画像と時間を一度だけ選ぶ。

| 選択 | 表示画像 | 1セット |
| --- | --- | ---: |
| `MY CARROT` | 自分の画像 | 60秒 |
| `HIT 8` | 相手の画像 | 70秒 |
| `CRITICAL 9` | 相手の画像 | 80秒 |
| `PERFECT 10` | 相手の画像 | 90秒 |

8・9・10は相手へ送る採点ではなく、本人が引き受ける追加運動時間の選択である。1～7や低評価は送らず、相手画像が刺さらなければ自分の本命画像で60秒を行う。選択は両者がロックするまで相手から読めず、確定後もRATE、ランキング、Pay、公開プロフィールへ流さない。

## 3セットの進行

1. 両者が表示名、好きな画像1枚、運動強度、配慮してほしい条件を準備する。
2. P2Pで画像を交換し、各自がにんじんを秘密選択する。
3. 指示役は運動名、完了条件、短い掛け声、任意のBPMを送る。
4. 実行役が開始すると、本人が選択した画像の上に進捗バーを表示する。
5. 最初の60秒を`BASE`とし、60秒到達でそのセットを基本完遂として記録する。
6. 70～90秒を選んだ場合、追加10～30秒を`BOOST`として続ける。選択時間まで到達すればBOOST完遂、BASE後に終えてもBASE完遂は取り消さない。
7. 相手の実行中、もう一方は`COACH & RECOVERY`となる。休憩時間を確認しながら、P2Pの短い掛け声で応援する。
8. ターン終了後は役割を自動交代し、継続投票を挟まず6ターンまで進む。

BASE前のギブアップは、その実行役の負けとして終了する。双方が3セットずつBASEを完遂した時だけペア完遂とする。一方が途中で終了しても、それ以前に本人が完遂したBASEセットは個人記録と当日進捗に残す。

## 指示データ

- 運動名: 40文字以内
- 完了条件: 60文字以内
- 掛け声: 120文字以内
- BPM: 未指定または40–160
- 1回あたりの拍数: 1、2、4
- 同じ相手への2・3セット目は、前回の指示を初期値として引き継ぎ、必要な箇所だけ変更できる

このモードは、互いに現実的な指示を出して役になりきる信頼ベースの遊びとして設計する。痛み、めまい、体調の変化を感じた場合は勝敗より中止を優先する。

## 同期とプライバシー

- 公開待機列へ置くのは匿名UID、端末セッションID、表示名、運動強度、プロトコル、バリアント、時刻、待機状態だけとし、配慮条件と画像は置かない。
- 配慮条件は招待を受けた本人が、参加者2人だけ読めるルームへ承諾と同時に書き込む。
- にんじん選択は本人だけが一度書ける。相手の選択は双方の選択がそろうまで読み取れない。
- Realtime Databaseへ保存するのはマッチング、配慮条件、にんじん選択、指示、開始時刻、BASE・BOOST・終了時刻だけとする。
- 画像は1280px以内のWebPへ端末内変換し、WebRTC DataChannelで相手へ直接送る。Firebaseへ保存しない。
- 応援は定型の短い掛け声と、各ターン1回・40文字以内の自由なロールプレイ応援をP2Pでその場だけ表示し、Firebaseや対戦後記録へ保存しない。
- 両者の画像交換が45秒以内に完了しなければ、指示ターンを始めずNO CONTESTとして終了する。
- メトロノームは自由卓の共通コントローラを再利用し、双方の拍を同期する。タイマーの正本はRealtime Databaseのサーバー時刻とする。

## サーバー確定と個人記録

Cloud Functionsの`trainingAction`が、ルーム参加者、にんじん選択、固定6ターンの役割交代、各ターンの画像所有者、BASE・BOOST境界、開始・終了時刻を再導出する。確定結果はユーザー専用の`trainingProfiles`へ保存し、同じルームの二重計上は`trainingClaims`で防ぐ。

個人記録には、参加回数、BASE完遂セット数、運動秒数、BOOST完遂数・秒数、3セット完遂回数、活動日数と連続活動日数を保存する。旧版の「1セット以上で増える活動日」と、新版の「3セット到達日・連続日数」は別項目で管理し、過去記録の意味を変えない。

専用デイリーミッションは本人のBASE 3セットで達成する。同じ日の複数ルームで完遂したセットを3まで加算し、日付をまたいだセッションは各BASEの完遂時刻ごとに日を分けて前日分を翌日へ移さない。旧版ですでに保存された`trainingSets: 1`は1セット分として保持する。相手の途中終了や再確定で本人の済みセットを失わせず、claimにより同じルームを二重加算しない。

## V1互換と一時データの清掃

V2は`protocolVersion: 2`と`kitaeai_60_v2`の待機列だけでマッチし、キャッシュ中のV1クライアントとは組み合わせない。新規V1ルームの作成は止めるが、配備時点ですでに進行しているV1ルームは、旧来の60秒以内コンプリート・継続投票の意味のままFunctionsとRulesで確定できるようにする。

`cleanupTrainingRooms`を5分ごとに実行し、自然に決着したV1・V2ルームは、先に両参加者のプロフィール・claim・デイリーをサーバー確定する。確定済みルームは24時間、破棄・期限切れルームはtombstoneから1時間だけ保持する。待機列は`sessionId`で別タブ・別端末を区別し、`lastSeen`から10分を超えた項目を回収する。

## 終了したふたりチャレンジとの境界

旧`team`系の待機列、招待、ルーム、チャット、採点領域は新規書き込みを禁止する。過去の戦績、実績、購入済み称号、デイリー受取記録は削除せず「終了モードの記録」として保持する。
