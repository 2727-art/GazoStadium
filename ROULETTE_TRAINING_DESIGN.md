# ルーレットトレーニング

Status: implementation contract
Scope: solo roulette training, 1〜10 local images, six BPM effects plus all-out count override, self-reported clear / give up, creator menu-pack marketplace, paid-use sales aggregation
Out of scope: P2P, camera or motion judging, medical or fitness claims, server-side workout records, RATE, competitive workout ranking

この文書は、2026年9月4日時点のワーキングツリーにある `roulette-training.js`、`roulette-training-core.mjs`、`functions/roulette-training.js`、`functions/roulette-training-service.js`、Firestore Rules／Indexesを正本としてまとめた実装契約である。本番デプロイ済みであることを示す文書ではない。

## 1. Product promise

「ルーレットトレーニング」は、好きな画像を見ながら縦ドラム式ルーレットを回し、メトロノームのBPMを動く速さの目安として、自分の判断でメニューへ取り組む完全ソロモードである。

- 画像はトレーニング中の舞台として使う。
- メインルーレットは「6つのテンポ効果」「おーるあうとたいむ♡」または「選択中のパックにあるトレーニングメニュー」を決める。
- メニューが決まった後に、別の回数ルーレットで正式な回数・秒数・往復数・セット数を決める。
- 作者の応援台詞は画像上の吹き出しへ表示する。
- メトロノームは運動強度そのもの、心拍数、フォーム、達成判定ではなく、動く速さの目安である。
- クリアは本人の自己申告であり、カメラ、マイク、モーションセンサー、AI解析で判定しない。
- ギブアップにペナルティを設けない。
- 画像、BPM、抽選結果、回数、クリア、ギブアップ、トレーニング結果を作者やサーバーへ送らない。

既存の通常1on1、戦略型1on1、貼り合い自由卓、推し値市場、AnjuPayフリマ、AI文字コラトレーニング、断惑NOTE、アカウント画面とは表示状態を共有しない。各モードは相互排他ガードで、ルーレットトレーニング中の重複起動を拒否する。

## 2. MVPのプレイループ

1. 無料公式パック、作者の端末内下書き、または市場の販売パックを選ぶ。
2. 端末内の画像を1〜10枚選ぶ。
3. 開始BPM、BPM上限、休憩時間、目標クリア数を選ぶ。
4. 画像の下にあるメインルーレットを回す。
5. 特殊効果が出た場合は効果を確定し、続けて必ずメニューを抽選する。
6. メニューが出た場合は、対応する画像と最初の応援台詞を確定する。
7. 回数ルーレットを回し、メニューの構造化された単位に対応する数値を確定する。
8. メニュー、補足、回数、BPMを確認し、3秒カウントダウンから開始する。
9. 本人が「クリア」または「ギブアップ」を選ぶ。
10. クリア後、設定した休憩を終えると再びメインルーレットを回せる。
11. 目標クリア数に達すると結果へ進む。ギブアップした場合も直ちに結果へ進む。
12. 結果を確認し、「記録を閉じてお部屋に戻る」で端末内セッションを破棄して入口へ戻る。

無料公式パックはFirebase市場へ接続できない場合も利用できる。有料パックの販売・利用開始・再開・終了・通報だけは、認証済みCallableを通す。

## 3. 画像と端末内プライバシー

### 3.1 選択・処理

- 選べる画像は1〜10枚である。
- 共通画像処理が利用できる場合は、1ファイル15MiB以下を検証し、長辺最大1,600px、品質0.86のWebPへ端末内で変換する。
- 画像はFirebase Authentication、Firestore、Realtime Database、Cloud Functions、Cloud Storage、市場の作者へ送らない。
- ファイル名、画像内容、画像から推測した情報を抽選へ使わない。
- 画像の代替テキストは「選択画像N」または「トレーニング用に選択した画像」とし、内容や人物名を推測しない。

### 3.2 保存同意

「再読み込みに備えて画像をこの端末へ保存する」は初期状態でOFFである。

- OFFでは画像Blobを永続保存せず、現在のページのメモリとBlob URLだけで使う。
- 本人がONへ切り替えた場合だけ、現在の画像BlobをIndexedDB `hariai-roulette-training-images-v1 / session-images / active`へ保存する。
- ONからOFFへ戻した時は、保存済み画像をIndexedDBから削除する。
- ONでも画像はサーバーへ送られない。
- 「記録を閉じてお部屋に戻る」では、ON/OFFにかかわらず今回のIndexedDB画像レコードとBlob URLを破棄する。これは長期ロスター保存ではなく、進行中セッションの再読込復旧用である。
- 保存セッションとIndexedDB画像には、認証中は `ownerUid`、完全ローカル利用では端末内ランダム識別子 `localOwnerId`を付ける。別の所有者として開いた復旧データは表示せず、端末から破棄する。

### 3.3 画像抽選

- メニューが確定した時に、メニュー抽選とは独立した画像バッグから1枚を選ぶ。
- 選択画像を一巡するまでは同じ画像を再使用しない。
- 新しい一巡の先頭が直前画像と同じにならないようにする。
- 画像が1枚だけの場合は、すべてのメニューで同じ画像を使う。
- 特殊効果だけが出た時は画像を進めず、その後にメニューが決まった時だけ画像を確定する。

## 4. 開始設定と既定値

| 設定 | 選択範囲 | 初期値 | 実装上の意味 |
| --- | --- | --- | --- |
| 画像 | 1〜10枚 | 未選択 | 1枚以上で開始可能 |
| 開始BPM | 40〜100、5刻み | 60 | セッションの持続BPM初期値 |
| BPM上限 | 40〜100、5刻み | 100 | テンポアップと一時効果の上限 |
| 休憩 | なし / 15秒 / 30秒 | 15秒 | 目標未達のクリア後に入る休憩 |
| 目標クリア数 | 3 / 5 / 10 | 5 | この回数の自己申告クリアで結果へ進む |
| 画像の端末保存 | OFF / ON | OFF | 進行中セッションの再読込用IndexedDB保存 |

- 開始BPMは選択したBPM上限を超えられない。
- BPMはすべて5刻みに丸め、40未満と選択した上限を超える値を使わない。
- 設定は専用localStorage `hariai-roulette-training-config-v1`へ保存し、ほかのモードの設定へ波及させない。
- 目標はラウンド数ではなく、本人が「クリア」を押した回数である。

## 5. ルーレットの構成

### 5.1 メインルーレット

メインルーレットは、画像ステージの下でパチンコのドラムのように縦回転する。

- 通常抽選では、メニューカテゴリ65%、特殊効果カテゴリ35%でカテゴリを先に決める。
- 65%／35%はパック内メニュー数にかかわらず固定する。10メニューのパックが効果カテゴリを押しのけることはない。
- メニューと7効果は、それぞれ独立したシャッフルバッグを使う。
- 各バッグは現在の一巡を使い切るまで同じ項目を重ねて出さない。
- メニューと効果は直前IDも保存し、新しい一巡の先頭が直前の結果と同じにならないようにする。
- 特殊効果が出た後は `forceMenu` を立て、次のメイン抽選を100%メニューにする。効果を連続させない。
- メニュー結果では、対応する画像インデックスと最初の応援台詞も回転演出を始める前に `pendingMain`へ固定し、再読込で別結果へすり替えない。
- 通常の回転演出は1,450msである。`prefers-reduced-motion`では待ち時間を0にし、ドラムアニメーションも停止する。

### 5.2 6つのテンポ効果とおーるあうとたいむ♡

| ID | 表示名 | 種類 | 効果 |
| --- | --- | --- | --- |
| `tempo_up` | テンポアップ | 持続 | 基本BPMを+5 |
| `tempo_down` | テンポダウン | 持続 | 基本BPMを-5 |
| `fever` | フィーバータイム | 一時 | 次のトレーニング開始から20秒、基本BPM×1.3 |
| `slow` | スロータイム | 一時 | 次のトレーニング開始から20秒、基本BPM×0.7 |
| `tempo_up_2` | テンポ2段階アップ | 持続 | 基本BPMを+10 |
| `tempo_down_2` | テンポ2段階ダウン | 持続 | 基本BPMを-10 |
| `all_out_time` | おーるあうとたいむ♡ | 回数上書き | 次のメニューの回数ルーレット7枠をすべて100にする |

共通ルール:

- 持続効果はセッションの基本BPM自体を変更し、以後のメニューにも残る。
- 一時効果は抽選結果画面では開始せず、次のトレーニングが実際に始まった時点から20秒を数える。
- 一時効果は基本BPMを変更しない。20秒後は現在の持続BPMへ戻る。
- 一時効果中に画面が非表示になった場合は残り時間を保存し、本人が再開した時点から残りを続ける。
- そのメニューをクリアまたはギブアップした時は、残っている一時効果も終了する。次のメニューへ持ち越さない。
- 乗算後も5刻みに丸め、下限40と本人が選んだBPM上限の間へ収める。
- 上下限に達して効果が数値を変えられない場合は、そのBPMのままであることを結果文へ表示する。
- `おーるあうとたいむ♡`はBPMを変えず、次に決まる1メニューだけへ適用する。クリアまたはギブアップ後には持ち越さない。

### 5.3 回数ルーレット

作者が決めるのは単位だけで、正式な数値はゲームが決める。通常候補は単位にかかわらず次の7値である。

| 状態 | 7つのドラム枠 |
| --- | --- |
| 通常 | 10、20、25、30、35、40、50 |
| `おーるあうとたいむ♡`適用中 | 100、100、100、100、100、100、100 |

- 候補から1つを端末内乱数で選び、回転開始前に `pendingCount` として保存する。
- `おーるあうとたいむ♡`の適用状態も端末内セッションへ保存し、再読込後も全枠100と決定済みの100を維持する。
- 作者のメニュー本文、補足、応援台詞に数字が含まれていても、正式な回数、BPM、目標へ反映しない。
- 購入前の全文表示では、各メニューの単位と通常候補を表示する。
- `seconds`だけは画面内タイマーを動かす。0秒になっても自動クリアにはせず、メトロノームを止めて本人の「クリア」または「ギブアップ」を待つ。
- `reps`、`round_trips`、`sets`は自動計測しない。

### 5.4 応援台詞

- メニュー確定時に、そのメニューの1〜4台詞から最初の1つを端末内でランダムに選ぶ。
- トレーニング実施中は8秒ごとに、最初の台詞を起点として作者の入力順で次の台詞へ切り替える。重複する同文は表示用ローテーションでは1つにまとめ、1種類だけの場合は固定表示とする。
- ローテーションはトレーニング開始後だけ動かし、一時停止、画面非表示、時間メニュー終了待ち、休憩では停止する。再開時は端末内に保存した現在の台詞から続ける。
- 台詞の切り替えは表示だけに作用し、メニュー抽選、回数、BPM、クリア判定、課金、販売実績には影響させない。
- 台詞は選択画像上の吹き出しにテキストとして表示する。
- 台詞は回数指示ではなく応援表現である。本文中の数字もゲーム命令として解釈しない。
- HTMLとして挿入せず、表示前にエスケープする。

## 6. クライアント状態遷移

### 6.1 画面状態

```text
hub
├─ setup
├─ editor → editor_review
└─ market → pack_detail

setup
├─ 有料利用を画像選択前にgive_up → result（0クリア）
└─ 開始 → main_ready
→ main_spinning
├─ effect_result → main_spinning（次は強制メニュー）
└─ menu_result
   → count_ready
   → count_spinning
   → count_result
   → countdown
   → active
      ├─ clear → rest → main_ready
      ├─ clear（目標到達）→ result
      ├─ visibility hidden → paused → active
      └─ give_up → result

countdown / paused / rest
└─ give_up → result

result
└─ end-training → 端末内状態を削除 → hub
```

- `main_spinning`と`count_spinning`は演出中状態であり、決定済みの `pendingMain`／`pendingCount` を保持する。
- `おーるあうとたいむ♡`確定時は `pendingAllOutCount` を保存し、強制メニュー確定時に `currentAllOutCount`へ移す。回数確定後も次のラウンドへ進むまでは、保存済み `pendingCount.candidates`の全7枠100を表示する。
- `countdown`は3秒である。画面が隠れた場合は中止して `count_result`へ戻し、自動で開始しない。
- `active`だけがメトロノームとトレーニング実時間を進める。
- `paused`ではメトロノーム、秒数タイマー、一時効果タイマーを止め、本人の「再開」を待つ。
- `rest`は設定秒数の終了、または「休憩を終える」で `main_ready`へ進む。
- 休憩なしではクリア直後に `main_ready`へ進む。

### 6.2 純粋ロジックとの対応

`roulette-training-core.mjs`の検証用状態機械は、UI演出状態を次の大きな状態へまとめる。

| 純粋ロジック | ブラウザUIのおもな対応 |
| --- | --- |
| `roulette` | `main_ready` / `main_spinning` |
| `effect_result` | `effect_result` |
| `menu_result` | `menu_result` / `count_ready` / `count_spinning` |
| `ready` | `count_result` / `countdown` |
| `training` | `active` |
| `paused` | `paused` |
| `rest` | `rest` |
| `result` | `result` |
| `ended` | ブラウザでは保存状態を削除して `hub`へ戻る |

純粋ロジックは遷移、抽選バッグ、冪等イベント、復旧データの整合性をテストする。ブラウザは同じ抽選・BPM・回数ヘルパーを使い、回転演出や3秒カウントを追加する。

## 7. クリア、ギブアップ、結果、終了

### 7.1 クリア

- `active`中に本人が押した「クリア」だけで `completedCount`を1増やす。
- カメラ、時間経過、メトロノーム、作者台詞は自動クリアの根拠にしない。
- `seconds`の時間切れも自動クリアにしない。
- 目標未達なら休憩設定に従い、次のルーレットへ戻る。
- 目標に達したクリアでは休憩を挟まず、結果へ進む。

### 7.2 ギブアップ

- 有料利用の準備画面には、画像を用意できない場合も利用を終了できる「ギブアップ」を置き、0クリアの結果へ進める。
- `countdown`、`active`、`paused`、`rest`には明示的な「ギブアップ」を置く。
- トレーニング画面からホームへ戻ろうとした場合も、確認後にギブアップとして結果へ進む。
- ギブアップは現在の音、回転待ち、カウント、運動タイマー、一時効果を止める。
- クリア済み回数を減らさず、Pay、RATE、売上ランキングの罰則を発生させない。
- 有料パックは利用開始時に成立済みのため、ギブアップしても返金せず、追加課金もしない。

### 7.3 結果

結果画面へ端末内で表示する値:

- クリアしたメニュー数
- 実際に `active`だった合計時間
- 開始時BPM
- 終了時の持続BPM
- セッション中の最高BPM
- `completed`または`give_up`の終了理由
- 最後に挑戦したメニュー、抽選数、単位、その挑戦開始時のBPM

結果は自己申告による端末内表示であり、作者、販売ランキング、RATE、AnjuPay、Firestoreへ送らない。ルーレット、準備、休憩、一時停止、結果待ちの時間は `active`時間へ加えない。

有料利用では結果へ入った時点で、結果画面を待たせず `finish_use { useId }`をベストエフォート送信する。送るのは利用IDだけで、結果値や終了理由は含めない。

### 7.4 終了

- 結果画面の「記録を閉じてお部屋に戻る」を唯一の通常終了操作とする。
- 端末内セッション、結果、画像Blob URL、IndexedDBの今回画像を破棄して入口へ戻る。
- 有料利用のサーバー終了が未確定なら、同じ `useId`だけを再送する。
- `finish_use`は結果を受け取らず、サーバー側の利用状態を `finished`にして `finishedAt`を記録するだけである。

## 8. 端末内保存と復旧

### 8.1 保存対象

進行中セッションはlocalStorage `hariai-roulette-training-session-v1`へschema version 1として保存する。

- 選択パックの固定スナップショット
- 開始設定
- 現在phase
- 開始・現在・最高BPM
- クリア数と実active時間
- メニュー／効果／画像の残りバッグ
- 直前のメニューID、効果ID、画像インデックス
- 効果後にメニューを強制する状態
- 決定済みメニュー、補足、応援台詞、回数
- 決定済み画像のインデックス
- 一時効果と秒数タイマーの残り時間
- 最後に挑戦したメニュー
- 有料の場合の `paidUseId`
- 結果と終了理由

画像本体はこのJSONへ入れず、本人が画像保存をONにした場合だけ別のIndexedDBへ置く。

### 8.2 復旧ルール

- 結果画面は同じ結果のまま復旧する。
- 運動中と一時停止中は `paused`へ復旧し、自動再開しない。
- 3秒カウントダウン中は `count_result`へ戻し、本人が「3秒カウントで開始」をもう一度押すまで開始しない。
- 画面非表示で停止した秒数タイマーと一時効果は、保存した残り時間から本人操作で再開する。
- メイン／回数ドラムの回転中に再読込した場合は、保存済み `pendingMain`／`pendingCount`を即時確定し、乱数を引き直さない。
- メニュー決定、回数決定、効果結果は同じ状態を復元する。
- 休憩中の再読込は休憩を再開せず、次の `main_ready`へ戻る。
- schema versionまたはパックが不正な場合は復旧に失敗し、保存セッションを削除する。
- 入口に復旧候補があり、まだメモリ上のセッションを開いていない状態でサイトホームへ戻る場合は、localStorageの復旧候補とIndexedDB画像を維持する。

画像保存がOFFで再読込し、画像だけを復元できない場合:

1. 端末内の途中phaseを破棄する。
2. 同じ固定パックと、存在する場合は同じ `paidUseId`を維持する。
3. 準備画面へ戻して画像の再選択を求める。
4. 新しいメニュー／画像／回数抽選を始めるが、有料利用を再課金しない。

### 8.3 有料利用の復旧

- 決済時のパック改訂全文を `rouletteTrainingUses/{useId}.packSnapshot`へ固定する。
- 現在の公開パックが改訂または非公開になっても、開始済み利用は同じスナップショットを再開する。
- `state`は本人の開始済み利用を返し、市場画面から `resume_use { useId }`を呼べる。
- `resume_use`はbuyer UID、`active`状態、本人のactive-use pointerを検証し、追加課金しない。
- 端末内の有料復旧候補を開く時も、保存した `ownerUid`を現在UIDと照合したうえで `resume_use`を呼び、サーバーが返した固定スナップショットへ置き換える。
- 同じ購入者は開始済み・未終了の利用を同時に1件だけ持てる。
- 終了通知に失敗した場合はlocalStorage `hariai-roulette-training-finish-retry-v1`へ `{ useId, ownerUid }`を置く。Functionsへ再送するpayloadは引き続き `{ useId }`だけで、運動結果は含めない。同一`useId`の送信は1つのPromiseを共有する。
- 終了確認中の`useId`は再開候補から除外し、再送が成功するまで新しい有料利用、市場、販売操作を遮断する。公式無料パックの端末内利用は遮断しない。
- owner UIDが現在UIDと異なる終了再送レコードは送信せず破棄する。旧形式のownerなしレコードは、現在UIDの`state.activeUse`が同じ`useId`であるとサーバー確認できた場合だけowner UIDを補い、再送対象にする。
- 端末内の無料復旧候補がある間は、復元または確認付きの完全破棄（JSONとIndexedDB画像）の二択とし、新しい画像で単一IndexedDBレコードを上書きしない。有料復旧候補は安易に破棄せず、同じactive useなら`resume_use`経由で復元する。別のactive useを再開する時は、競合する旧JSONと画像の破棄完了を先に確認する。

## 9. 販売パック

### 9.1 商品単位

商品は、1人の作者が作る1〜10件のトレーニングメニューをまとめた「パック」である。メニュー1件ごとの個別販売にはしない。

パック項目:

| 項目 | 条件 |
| --- | --- |
| 作者名 | 1行、1〜16文字 |
| パック名 | 1行、1〜30文字 |
| 紹介文 | 10〜120文字、改行可 |
| 価格 | 5 / 10 / 25 Pay |
| メニュー数 | 1〜10件 |

各メニュー項目:

| 項目 | 条件 | ゲームへの作用 |
| --- | --- | --- |
| `itemId` | サーバー生成 | 同じ配列位置を改訂・通報・復旧で追う識別子 |
| `menuText` | 必須、1行、最大80文字 | メニュー名として表示 |
| `detailText` | 任意、最大120文字、改行可 | やり方・補足として表示 |
| `countUnit` | 必須、4固定値の1つ | 回数ルーレット候補を選ぶ唯一の構造化項目 |
| `cheerLines` | 必須、1〜4行、各1行最大60文字 | 画像上の応援台詞候補 |

- `menuText`、`detailText`、`cheerLines`は作者の自由入力を尊重する。
- 自由入力内の数字は禁止しない。ただし正式な回数、BPM、休憩、目標には反映しない。
- クライアントから `itemId`を指定させず、初回公開で `item-01`からサーバー生成する。
- 改訂時は同じ位置の `itemId`を維持し、追加された位置にだけ新しい連番を付ける。
- 現行契約は位置単位の維持である。削除や並べ替えで位置が変わったメニューの論理IDまで維持する方式ではない。
- 公開応答は移行用として同じ値を `itemId`と `id`の両方で返す。
- 下書きはlocalStorage `hariai-roulette-training-draft-v1`へ保存し、作者は公開前に端末内で無料テストできる。

### 9.2 購入前全文表示

購入前詳細では、次を省略せず表示する。

- 作者名、パック名、紹介文
- 全メニューの本文
- 全メニューの補足
- 全メニューの回数単位と通常候補
- 全応援台詞
- パック価格
- 現在残高
- 利用開始後残高
- 1セッションの使い切り条件
- 65%メニュー／35%効果と、効果後は必ずメニューになる抽選ルール
- クリア・ギブアップ・抽選回数による追加料金がないこと

価格、改訂、残高のいずれかが確認画面から変わった場合は、決済せず最新の全文確認へ戻す。

### 9.3 公開・改訂・非公開

- 新規公開と改訂公開は、同じ `publish`アクションを使う。
- 新規は `baseRevision: 0`、改訂は現在の `packId`と `baseRevision`を送る。
- サーバーは現在revisionを照合し、競合した古い編集を上書きしない。
- 公開・改訂ごとに完全なrevisionスナップショットを保存する。
- 作者は販売受付を非公開にできる。非公開は開始済み利用を失効させない。
- 公開操作は `actionId`とpayload hashで冪等にし、同じ操作の再送で公開料を二重に取らない。
- 1作者が所有できるパックは、公開中・非公開・隔離中を合わせて最大50件である。非公開化は所有数を減らさない。
- 所有数は `rouletteTrainingSellerStats/{sellerUid}.packCount`を正本とし、新規公開時に同じFirestoreトランザクション内で上限確認と加算を行う。上限到達後も既存パックの改訂はできる。
- 自分のパック一覧はFirestoreで `updatedAt`降順に最大50件を取得するため、所有中の全パックを管理できる。

### 9.4 自由入力の安全境界

公開時にFunctionsで、構造、文字数、制御文字、HTMLに見える `<`／`>`、連絡先、URL、個人情報、外部取引、成人向け／規制対象、危険な運動強制、中止妨害、人格否定・脅迫、定義済みの性的表現を検査する。

とくに次は公開不可とする。

- 痛み、めまい、息苦しさ、吐き気、体調不良を無視・我慢させる表現
- 倒れるまで、限界を超えて、意識を失うまでなどの極端な指示
- 水分、呼吸、休憩、中止、ギブアップを妨げる表現
- 定義済みの危険な姿勢・場所での運動指示
- 人格否定、脅迫、暴力、性的表現
- 個人情報、外部連絡先、URL、外部取引・外部決済・実物受け渡しへの誘導

自動検査は安全性や権利を完全に保証する審査ではない。表示時エスケープ、購入前全文確認、購入者通報、サーバー非公開を組み合わせる。

### 9.5 通報と隔離

- 通報単位は `packId + revision + reporter UID`である。
- 通報できるのは、その改訂を実際に有料利用した購入者だけである。作者の自己試遊と未購入閲覧は購入者証跡にならない。
- 通報時は理由と、対象revisionの全文スナップショットを保存する。
- 同じ購入者による同じrevisionへの再送は冪等である。
- 高危険度理由は `dangerous_exercise`、`stop_obstruction`、`harassment`、`sexual`、`privacy`である。`harassment`は人格否定・脅迫・差別を含む。
- 現在公開中の同一revisionへ、異なる購入者3人の高危険度通報が成立した時、そのrevisionを `hidden / quarantined`へ変更する。
- `rights`、`external_trade`、`other`も証跡として保存するが、現行の自動3件隔離カウントには含めない。
- 隔離された安全関連の公開文面と同一のまま、revision番号や `countUnit`だけを変えて再公開することはできない。差分判定hashはパック名、紹介文、メニュー本文、補足、応援台詞だけを対象にする。
- 有料利用開始時に `packId + revision + buyer UID`ごとの `rouletteTrainingPackRevisionBuyers`証跡を1行だけ作り、購入時の全文スナップショットを固定する。作者自己試遊では作らない。
- 有料利用の終了時に、その1行の `lastUseId`と `lastUsedAt`を最新の終了済み利用へ更新する。`state`はこの証跡を `lastUsedAt`降順で最大20改訂返すため、同じ改訂の反復利用で古い別改訂が一覧から押し出されない。
- 購入済み改訂の応答は従来の公開利用形（`id`、`status`、`charged`、決済額、時刻、`pack`）を維持し、作者が改訂・非公開化した後も、その固定全文から通報できる。

## 10. AnjuPayと一回利用

### 10.1 価格と手数料

AI文字コラトレーニング台本市場と同じ固定価格・手数料を使う。

- 販売価格: 5 / 10 / 25 Pay
- 新規公開・改訂公開: 1 Pay
- 利用成立手数料: 20%、最低1 Pay、端数切り捨て
- 作者受取: 価格から利用成立手数料を引いた額

| 価格 | 手数料 | 作者受取 |
| ---: | ---: | ---: |
| 5 Pay | 1 Pay | 4 Pay |
| 10 Pay | 2 Pay | 8 Pay |
| 25 Pay | 5 Pay | 20 Pay |

公開時と利用開始時は、確認画面で見た残高をFirestoreトランザクション内でも照合する。残高、価格、revision、作者、公開状態、モデレーション状態の検証が1つでも失敗した場合は、決済・作者付与・利用開始・ランキング集計をまとめて成立させない。

### 10.2 販売成立タイミング

有料販売は、購入前全文確認後に `start_paid_use`のFirestoreトランザクションが成功した時点で成立する。現行UIでは、この成立後に画像・BPM・目標を選ぶ準備画面へ進む。

同じトランザクションで次を行う。

- 購入者から価格を引く。
- 作者へ手数料控除後の売上を付ける。
- buyer／seller双方のAnjuPay台帳を作る。
- 固定revisionのパックスナップショットを持つ `useId`を `active`で作る。
- 本人のactive-use pointerを作る。
- 実売上、月間集計、ランキング上限用のペア証跡を更新する。
- 購入者がそのrevisionを利用した証跡を作る。

クリア数、抽選回数、経過時間、ギブアップ、最終結果は販売成立条件にしない。これにより、運動結果を作者・サーバーへ送らずに販売を確定できる。

### 10.3 1セッション1課金

- 1回利用は `start_paid_use`から `finish_use`までの1つの `useId`である。
- メイン抽選、回数抽選、メニュー数、応援台詞表示、クリア、休憩ごとに課金しない。
- クライアント生成 `actionId`から決定的な `useId`を作り、同じ内容の再送は同じ利用を返す。
- 同じ `actionId`を別のパック、価格、revisionへ再利用した場合は拒否する。
- 再読込は同じactive useを `resume_use`し、再課金しない。
- 終了済み `useId`は再開できない。もう一度使う場合は、新しい全文確認と新しい利用開始が必要である。
- ギブアップでもその1回利用は使い切りとなるが、追加請求と失敗ペナルティはない。
- 決済後の準備画面で画像を用意できない場合も、同じ `useId`を0クリアのギブアップ結果として終了できる。
- 作者が自分の公開パックを試す場合は `charged: false`とし、購入者証跡、売上、手数料、利用数、ランキングへ加えない。
- 作者の未公開下書きテストは完全に端末内で行い、サーバー利用自体を作らない。

## 11. 売上・ランキング集計

ルーレットトレーニングの「ランキング」は運動結果ではなく、販売パックの有料利用開始を集計する。クリア数、BPM、画像、回数、ギブアップを順位へ使わない。

### 11.1 累計と月間

作者ごとに次をサーバー正本として持つ。

- `packCount`: 公開状態にかかわらない所有パック数。累計seller statsだけに持ち、新規公開上限50件の判定に使う
- `actualGross`: ランキング上限に関係なく、成立した全有料利用の価格合計
- `rankingGross`: ランキング対象になった利用だけの価格合計
- `netSales`: 作者の実受取合計
- `feesPaid`: 市場手数料合計
- `useCount`: 成立した全有料利用件数
- `rankingUseCount`: ランキング対象利用件数
- `uniqueBuyers`: 累計または月間の異なる有料購入者数

累計は `rouletteTrainingSellerStats/{sellerUid}`、JST月間は `rouletteTrainingMonthlyPeriods/{YYYY-MM}/entries/{sellerUid}`へ保存する。

### 11.2 同一購入者の上限

- 同一購入者から同一作者への有料利用は、JST同日1件だけ `rankingGross`と`rankingUseCount`へ加える。
- 2件目以降も購入者の支払い、作者受取、手数料、`actualGross`、`netSales`、`feesPaid`、`useCount`は通常どおり全件処理する。
- 自己試遊は実売上にもランキングにも加えない。
- 生涯ユニーク購入者と月間ユニーク購入者は、それぞれ別のbuyer→sellerペア証跡で一度だけ加える。
- ランキング対象かどうかは購入完了やクリアではなく、サーバー時刻による利用開始時点で決める。
- ランキングを理由にPay報酬や購入者還元を行わない。

### 11.3 人気パックランキング

市場には月間／累計の「人気パックランキング」を各TOP20まで表示する。これは作者売上ランキングではなく、現在販売中の個別パックを見つけるための一覧である。

- 主順位は、現在の内容を有料利用した異なる購入者数 `uniqueBuyers`の降順とする。
- 副順位は、同一buyer→同一パック内容についてJST同日1件だけ数える `rankingUseCount`の降順とする。
- 両方が同じ場合は同順位とし、表示順位は `1, 2, 2, 4`形式にする。内部の安定順はそのスコアへ到達した時刻が早い順、最後に `packId`順とする。
- 掲載には `uniqueBuyers >= 3`かつ `rankingUseCount >= 1`が必要である。作者ダッシュボードでは未達のパックに「あとN人」を表示する。
- `active / clear / isCurrentPublic`をすべて満たす現在内容だけを候補にし、受付停止、安全確認中、改訂前の内容は公開一覧へ出さない。
- 価格、売上額、手数料、作者本人の試遊、購入後のクリア、ギブアップ、BPM、回数、画像は順位へ使わない。
- ランキング閲覧時だけCallableから取得し、常時購読や自動ポーリングを行わない。

### 11.4 パック改訂と集計単位

人気集計は `packId`だけでなく、次の現在内容から作る決定的な `rankingContentHash`と `packRankingId`を単位にする。

- メニュー名 `menuText`
- 補足 `detailText`
- 回数単位 `countUnit`
- 応援台詞 `cheerLines`

上記内容を改訂した場合は、新しい `packRankingId`を0件から集計し、改訂前の統計documentは履歴として保持したうえで `isCurrentPublic: false`にする。タイトル、商品説明、価格、作者表示名だけの変更ではランキング集計をリセットしない。受付停止または安全隔離では現在内容を非公開扱いにし、同じ安全な内容を再公開した場合だけ再び現在候補へ戻す。

### 11.5 販売実績コレクション

作者別の累計 `rouletteTrainingSellerStats`を正本とし、次の2系列を `scope: roulette_training`、`category: roulette_training_pack_sales`で解除する。

- パック利用成立: `rankingUseCount`が `1 / 3 / 10 / 30 / 100 / 300 / 500 / 1000 / 3000 / 10000`
- パックの利用者: `uniqueBuyers`が `3 / 10 / 30 / 100 / 200 / 300 / 500 / 1000 / 2000 / 3000`

販売成立と同じFirestoreトランザクションで作者の実績プロフィールを評価し、新規解除がある場合だけ更新する。既存集計は作者ダッシュボードや共通実績画面を次に開いた時にも再評価できる。自己試遊、冪等リプレイ、価格、売上額、ランキング順位、運動結果は解除条件へ使わず、実績によるPay報酬も発生させない。

### 11.6 ランキング用Xプロフィール

作者は `rouletteTrainingSellerProfiles/{sellerUid}`へルーレットランキング専用のX公開設定を任意保存できる。

- 初期値は非公開で、他市場や総合ランキングのX設定を継承しない。
- 自由入力URLを受け付けず、NFKC正規化後の英数字とアンダースコア1〜15文字のhandleだけを保存する。
- 公開応答は本人が `xPublic: true`にした有効なhandleだけを返し、クライアントが固定の `https://x.com/{handle}`を組み立てる。
- 外部移動前に、作者の自己申告であり運営が本人確認・リンク先確認をしていないことを表示する。X API、埋め込み、プロフィール画像、フォロワー数、クリック追跡は使わない。
- X公開の有無は順位、実績、販売、Payへ影響しない。OFFにしても集計は残り、ランキング行からリンクだけを外す。
- 安全確認中のパックが1件でもある作者はXを新規公開できず、高リスク通報でパックが隔離された時はルーレット専用Xを非公開にする。

## 12. Firebaseとサーバー境界

### 12.1 Callableアクション

すべて `rouletteTrainingAction`を通し、App Checkを初日から強制する。

| action | クライアントから送る主情報 | サーバーの役割 |
| --- | --- | --- |
| `state` | actionのみ | policy、残高、更新順の自分のパック、公開パック、active use、直近20件の購入済み改訂を返す |
| `browse` | actionのみ | `active / clear`を有料利用数降順、同数は更新日時降順で最大100件返す |
| `publish` | actionId、確認残高、パック本文、価格、revision | 検査、公開料、revision保存を原子的に行う |
| `unpublish` | packId | 作者本人の販売受付を停止する |
| `start_paid_use` | actionId、packId、確認revision、確認価格、確認残高 | 決済、固定snapshot、売上・ランキング集計を原子的に行う |
| `resume_use` | useId | buyer所有権とactive状態を確認し、固定snapshotを返す |
| `finish_use` | useId | statusを`finished`にし、finishedAtを記録する |
| `report` | packId、確認revision、理由 | 有料購入者証跡を確認し、通報と必要な隔離を記録する |
| `pack_rankings` | period（`monthly` / `lifetime`） | 現在公開中の内容を異なる購入者数優先で並べ、内部UIDを除いたTOP20を返す |
| `creator_stats` | actionのみ | 本人の月間／累計販売集計、各パックの掲載進捗、販売実績、X設定を返す |
| `save_profile` | xPublic、xHandle | ルーレット専用の任意X公開設定を検証して保存する |

各actionは許可キーを厳密に検査する。画像、画像ID、BPM、乱数、抽選結果、回数、休憩、目標、クリア、ギブアップ、運動結果、フォーム判定を追加で送ると拒否する。

### 12.2 Firestore namespace

```text
rouletteTrainingPacks/{packId}
rouletteTrainingPacks/{packId}/revisions/{revisionId}
rouletteTrainingUses/{useId}
rouletteTrainingActiveUses/{uid}
rouletteTrainingPublishActions/{actionIdHash}
rouletteTrainingPackRevisionBuyers/{revisionBuyerHash}
rouletteTrainingReports/{reportId}
rouletteTrainingSellerStats/{sellerUid}
rouletteTrainingMonthlyPeriods/{YYYY-MM}/entries/{sellerUid}
rouletteTrainingRankingPairs/{JST-date-pairHash}
rouletteTrainingSellerBuyerPairs/{pairHash}
rouletteTrainingMonthlyBuyerPairs/{monthPairHash}
rouletteTrainingPackStats/{packRankingId}
rouletteTrainingPackMonthlyPeriods/{YYYY-MM}/entries/{packRankingId}
rouletteTrainingPackRankingPairs/{JST-date-pack-pairHash}
rouletteTrainingPackBuyerPairs/{packBuyerPairHash}
rouletteTrainingPackMonthlyBuyerPairs/{monthPackBuyerPairHash}
rouletteTrainingSellerProfiles/{sellerUid}
```

このモード専用のRealtime Databaseパスは作らない。

### 12.3 公開情報と非公開情報

- Firestore Rulesは、上記すべてのdocument／collection queryについて、認証済み・未認証を問わずクライアントの直接read/writeを拒否する。
- Functions Admin SDKだけが読み書きし、必要な情報だけをCallable応答として返す。
- 公開パックは作者の内部UIDを返さず、ルーレット専用saltを含む決定的ハッシュ `publicSellerId`と作者表示名を返す。
- 人気ランキングも `sellerUid`を返さず、`packId`、現在のタイトル・価格・revision、匿名化した `publicSellerId`、作者表示名、集計値、本人が任意公開したX handleだけを返す。
- `sellerUid`、`buyerUid`、`reporterUid`、`actionId`、payload hashは公開応答へ出さない。
- 市場へ出すパック本文は公開情報である。購入前全文表示を成立させるため、秘密商品として扱わない。
- UGC配列を持つpack／use／revision-buyer証跡／report／publish-actionの大きなフィールドは単一フィールドIndexから除外する。
- 市場一覧、自分のパック一覧、購入済み改訂一覧は、それぞれの絞り込みと並び順に対応する複合Indexを使い、取得後のメモリ内並べ替えへ依存しない。

## 13. 所有権、改訂、互換性

- パック所有者は認証UIDからサーバーが決め、クライアントに `sellerUid`を指定させない。
- 更新・非公開は同じseller UIDだけが行える。
- 有料利用はbuyer UIDに帰属し、`resume_use`と`finish_use`は本人だけが実行できる。
- 公開revisionを利用開始時に完全snapshot化するため、作者が後で変更・非公開化しても購入済み1セッションの内容は変わらない。
- schema versionは、ブラウザセッション、純粋ロジック、Firestoreパック／利用／集計とも現行 `1`である。
- 公開メニューは `itemId`を正本とし、現行ブラウザとの移行期間は同じ値の `id` aliasも返す。
- クライアントのpack正規化は `items`／`menus`、`menuText`／`training`／`exercise`／`title`、`cheerLines`／`dialogue`／`line`の既知aliasを読み取れるが、新規公開のFunctions入力は正規キーだけを許可する。
- アカウント移行では、開始済みルーレット利用をactive sessionとして扱う。また移行先UIDにルーレットのパック、利用、通報、active use、seller stats、または `rouletteTrainingSellerProfiles` の任意X公開設定があれば「空の移行先」とみなさない。
- AnjuPay台帳は公開料、利用料、作者売上へルーレット専用kind／labelを使い、AI文字コラや別市場の履歴と混同しない。

## 14. Accessibility and device behavior

- メイン操作はネイティブのbutton、input、select、textareaで構成する。
- 画面ごとに見出しと `aria-label`を持たせ、決定したメニューやBPM、開始、終了、一時効果終了を `role="status" aria-live="polite"`の低頻度領域へ通知する。
- 高頻度の250msタイマー更新やメトロノーム拍をライブ読み上げしない。
- 画像削除ボタンには「選択画像Nを外す」の個別ラベルを付ける。
- BPMは数値と「テンポ目安」の文字で表示し、色や脈動だけで伝えない。
- クリアとギブアップは別々の明示ラベルとし、主要判定ボタンは64px以上の高さを持つ。
- `prefers-reduced-motion`ではドラム待ちをなくし、CSSアニメーション、トランジション、smooth scrollを停止する。
- 820pxと620pxのbreakpointで、準備、市場、エディター、結果、画像一覧、吹き出し、縦ドラムを1列または小画面向けへ組み替える。
- 音は本人が「3秒カウントで開始」を押した時にAudioContextを初期化する。
- アプリ全体の音設定がOFF、AudioContextが利用不能、または再開が拒否された場合も、表示と自己申告操作だけで進行できる。
- メトロノームは `active`中だけ鳴らし、一時停止、休憩、結果、終了、秒数終了では止める。
- 画面非表示とpagehideでは進行中の運動を自動一時停止し、本人操作なしに音やタイマーを再開しない。
- ギブアップと結果への遷移は、localStorageやネットワーク障害があってもローカルの音・タイマー停止を先に行う。

## 15. 現行実装で確認する境界

この節は将来仕様の推測ではなく、上記コードを突き合わせて確認できる現在の制約である。

### 15.1 ランキング表示

市場の「人気ランキング」タブから、必要な期間だけ `pack_rankings`で月間／累計TOP20を取得する。作者ダッシュボードは `creator_stats`で本人の正確な販売記録、現在パックごとの掲載進捗、販売実績コレクション、ルーレット専用X設定を取得する。いずれも常時購読せず、表示開始または本人の更新操作時だけ読む。

### 15.2 ローカル復旧のアカウント境界

ブラウザセッション、作者下書き、IndexedDB画像は、認証UIDまたは完全ローカル用 `localOwnerId`へ結び付ける。別UID、所有者不明の有料セッション、認証状態と一致しない旧形式の復旧データは引き継がず、端末から破棄する。有料 `resume-local`は表示前に `resume_use`でbuyer所有権・active pointer・未終了状態を再検証する。モード表示中に認証UIDが変わった場合も、タイマーと音を停止し、旧UIDの画像・セッション・結果・下書きを即座に画面から外して、新UIDの状態を読み直す。したがって、アカウント変更時に旧セッションを自動移管する仕様ではない。

## 16. 完了条件

- 既存モードを開始・終了でき、ルーレットトレーニングと同時起動しない。
- 1〜10画像を選べ、保存同意OFFでは画像を永続化せず、ON時だけIndexedDBへ保存できる。
- 画像・BPM・抽選・回数・クリア・ギブアップ・結果がFirebase requestへ混入するとFunctionsが拒否する。
- 6つのBPM効果が5刻み、40〜本人設定上限、持続／20秒一時効果の規則どおり動き、`おーるあうとたいむ♡`が次の1メニューだけ全7枠を100にする。
- メニュー65%／効果35%をメニュー件数と独立して維持し、効果後に必ずメニューを出す。
- メニュー／効果／画像はバッグ内で重複せず、新しい一巡の先頭も直前結果との連続重複を避ける。
- 4単位共通の通常7候補（10、20、25、30、35、40、50）から回数を抽選し、自由入力中の数字を正式回数へ使わない。
- 回転中の再読込で決定済みメニュー、画像、応援台詞、回数を引き直さず、運動中は一時停止、カウントダウン中は開始前へ復旧する。
- クリアだけが目標進捗を増やし、ギブアップは直ちに音とタイマーを止めて端末内結果へ進む。
- 結果は実active時間、クリア数、BPM、最後の挑戦を表示し、作者、RATE、売上ランキングへ送らない。
- 1〜10メニューのパックを公開・改訂・非公開化でき、全自由入力と単位を購入前に確認できる。
- 作者の所有パックを合計50件に制限し、上限確認と `packCount`加算を新規公開・公開料と同じトランザクションで行う。
- 公開一覧を `active / clear`かつ有料利用数・更新日時順の上位100件、自分の一覧を更新日時順の全所有50件として取得する。
- 公開・改訂の1 Payと、5／10／25 Payの利用決済が、確認残高・価格・revisionを含む1トランザクションで成立する。
- 同じ `actionId`の再送と同じactive useの再開で二重請求しない。
- 作者自己試遊を無料にし、売上、購入者証跡、利用数、ランキングへ加えない。
- 購入済み改訂をbuyer×pack×revisionの1証跡にまとめ、最新終了日時順の20改訂を固定全文付き・内部UIDなしで再表示できる。
- 実売上は全件、ランキング売上は同一buyer→sellerのJST同日1件だけを累計・月間へ加える。
- 人気パックランキングは現在内容単位の異なる有料購入者数を主順位、同一buyer→同一内容のJST同日対象利用を副順位とし、同スコアを同順位で月間／累計TOP20へ表示する。
- メニュー名・補足・単位・応援台詞の改訂では新しい内容として集計し、旧内容を候補から外す。タイトル・説明・価格・作者名だけの変更では集計を維持する。
- 作者ダッシュボードで掲載までの残り人数、月間／累計集計、2系列20件の販売実績、任意X公開を確認・更新できる。
- ランキングと実績は価格、売上額、順位報酬、画像、BPM、回数、クリア、ギブアップへ依存せず、Pay報酬を発生させない。
- `finish_use`が `useId`以外の運動結果payloadを受け取らない。
- 異なる有料購入者3人による同一revisionの高危険度通報（人格否定・脅迫・差別を含む `harassment`を含む）で、現在revisionを隔離できる。
- ルーレット専用Firestore namespaceへのクライアント直接read/writeをすべて拒否し、CallableへApp Checkを強制する。
- reduced-motion、画面非表示の自動停止、無音継続、読み上げ通知、モバイルレイアウトが機能する。
- 純粋ロジック、入力検証、Functions取引、Rules配線、App Check配線、既存モード相互排他のテストが通る。
